from easydict import EasyDict as edict

config = edict()

# Пример конфигурации
config.bot = edict()
config.bot.owner = 6011272640  # ID владельца бота
config.groups = edict()
config.groups.main = -1002179725226  # ID главной группы
